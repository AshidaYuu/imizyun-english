{
  "name": "imijun-training-app",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "esbuild src/main.jsx --bundle --outfile=dist/app.js --format=iife --global-name=AppBundle --servedir=. --serve=5173",
    "build": "esbuild src/main.jsx --bundle --outfile=dist/app.js --format=iife --global-name=AppBundle",
    "preview": "esbuild src/main.jsx --bundle --outfile=dist/app.js --format=iife --global-name=AppBundle --servedir=. --serve=4173"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.21.5"
  }
}
